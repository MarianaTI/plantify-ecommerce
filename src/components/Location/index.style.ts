import styled from "@emotion/styled";

export const Container = styled.div`
  width: 500px;
  display: flex;
  flex-direction: column;
  h1 {
    color: var(--default);
    font-size: 16px;
    font-weight: 700;
  }
  span {
    color: var(--default);
    font-size: 14px;
    font-weight: 500;
  }
`;
